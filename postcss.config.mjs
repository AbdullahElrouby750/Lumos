// ESM PostCSS config for Tailwind
// this file uses ES module syntax, supported because package.json has "type": "module".

import autoprefixer from 'autoprefixer';

export default {
  plugins: {
    autoprefixer: {},
  },
};
